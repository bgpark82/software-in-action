spring:
  application:
    name: crew-dispatch

  datasource:
    url: jdbc:postgresql://localhost:5432/flix
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate # none?? ???? jpa? ddl ???? ???. validate? entity? table? ??? ??
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration # SQL file path
    baseline-on-migrate: false # true: flyway_schema_history ?? (version ???? ??, ?? ??? ??x), false: history ??, flyway? ????? ??? ??? ???? (false?? flyway_schema_history? ????. ????? ?? ???? ??)
    baseline-version: 0 # ??? version 1?? ????? version? 1?? ??? ???. ???? ?? ???? ??
    validate-on-migrate: true # ?????? ?? ?? ??, ?? ??????? ??? ?????? ????
    fail-on-missing-locations: true