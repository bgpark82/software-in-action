package com.bgpark

/**
 * Kotlin is named after island in Finland
 * - doesn't have semicolon
 */

/**
 * main
 * - entry point of kotlin application (top level function)
 * - args can be omitted
 */
fun main(args: Array<String>) {
    for (arg in args) {
        println(arg)
    }
}